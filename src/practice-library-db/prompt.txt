You are generating high-quality interview practice questions for a database with the following schema:

[SCHEMA BELOW]
-----------------
{{SCHEMA}}
-----------------

OUTPUT REQUIREMENTS
Generate {{BATCH_SIZE}} new questions.
They MUST follow this JSON shape exactly:

{
  "practice_questions": [
    {
      "id": "string",
      "type": "mcq | open | truefalse | matching | system-design",
      "difficulty": "entry | junior | middle | senior",
      "status": "published",
      "isDemoMode": false,
      "companyType": "faang | startup | enterprise",
      "title": "string",
      "description": "string",
      "prompt": "string",
      "topic": "frontend | backend | devops | system-design | algorithms | javascript | react | sql | networking | etc",
      "subtopics": ["..."],
      "tags": ["..."],
      "estimatedTimeMinutes": number,
      "aiEvaluationHint": "string or null",
      "multiChoiceAnswers": ["correct", "answers"] or null,
      "companies": [
         {
            "name": "string",
            "logo": "string",
            "size": "faang | startup | enterprise",
            "description": "string"
         }
      ] or null,
      "positions": ["..."],
      "primaryTechStack": ["react", "typescript", "nodejs", "..."],
      "interviewTypes": ["regular", "practice"],
      "seniorityLevels": ["entry", "junior", "middle", "senior"],
      "trueFalseCorrectAnswer": true | null,
      "trueFalseExplanation": "string or null",
      "matchingShuffleLeft": boolean or null,
      "matchingShuffleRight": boolean or null,
      "matchingPairs": [{"left": "...", "right": "...", "explanation": "..."}] or null,
      "openReferenceAnswers": [{"id": "string", "text": "string", "weight": 0.5}] or null,
      "createdBy": "system"
    }
  ],

  "practice_mcq_options": [
    {
      "id": "string",
      "questionId": "string",
      "text": "string",
      "isCorrect": boolean,
      "explanation": "string or null"
    }
  ],

  "practice_matching_pairs": [
    {
      "id": "string",
      "questionId": "string",
      "left": "string",
      "right": "string",
      "explanation": "string or null"
    }
  ],

  "practice_system_design_charts": [
    {
      "id": "string",
      "questionId": "string",
      "chart": [...]
    }
  ]
}

RULES
- IDs must be unique UUID-like strings.
- In a single batch do not create two questions with identical prompts.
- Avoid repeating the same concept with only small wording changes.
- Spread topics across different sub-areas of the given technology.
- Use only companies from this list:

{{COMPANIES}}
Google
Meta
Amazon
Apple
Netflix
Microsoft
Tesla
Nvidia
Oracle
Salesforce
IBM
Intel
Adobe
SAP
Cisco
X
LinkedIn
Snapchat
TikTok
Reddit
Amazon AWS
Microsoft Azure
Google Cloud
Cloudflare
Vercel
Stripe
Spotify
Uber
Airbnb
Gaming Realms
Plandek

CONTENT RULES
- Every question MUST include:
  title, description, prompt, topic, difficulty
- Favour entry and junior difficulty; include few middle; rarely senior.
- MCQ questions:
  - must output practice_mcq_options entries
  - must set multiChoiceAnswers with correct answer texts
  - all correct answers must be valid
  - each MCQ should have 3–5 options
- True/False questions:
  - trueFalseCorrectAnswer MUST always be true
  - must include trueFalseExplanation
- Open questions:
  - include 1–3 openReferenceAnswers samples
- Matching questions:
  - include 3–6 matchingPairs
  - must replicate matchingPairs into practice_matching_pairs
- System-design questions:
  - include at least one chart with nodes + relationships
  - keep diagrams simple for entry/junior difficulty
- Keep questions simple, readable and beginner-friendly.
- Must strictly output valid JSON only, no commentary.

FINAL INSTRUCTIONS
- Return **only JSON**, no explanations or markdown.
- Use a single JSON object with keys:
  "practice_questions", "practice_mcq_options", "practice_matching_pairs", "practice_system_design_charts"

- The output **must** be wrapped in:

```json
{ ... }
